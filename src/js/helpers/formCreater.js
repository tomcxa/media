export function formHTMLCreater(type) {
    let template = '';
    if (type === 'audio') {
        template = 
    }
    if (type === 'geolocation') {
        template = 
    }
    if (type === 'video') {
        template = 
    }
    return template;
}

export function geolocationValid() {
    return;
}
